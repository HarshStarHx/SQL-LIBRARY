<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete User</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #ffe6e6, #ffd9d9);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }

        .container {
            background-color: #ffffff;
            padding: 40px 30px;
            border-radius: 15px;
            box-shadow: 0 15px 30px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 450px;
        }

        h1 {
            text-align: center;
            color: #b30000;
            margin-bottom: 20px;
        }

        .readonly-info {
            background-color: #fce4e4;
            padding: 10px 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            color: #990000;
            font-weight: 500;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        .form-group {
            position: relative;
            margin-bottom: 25px;
        }

        input {
            width: 100%;
            padding: 14px 12px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
            outline: none;
            transition: all 0.3s;
        }

        input:focus {
            border-color: #ff4d4d;
            box-shadow: 0 0 8px rgba(255,77,77,0.2);
        }

        label {
            position: absolute;
            top: 50%;
            left: 12px;
            transform: translateY(-50%);
            background: white;
            padding: 0 5px;
            color: #888;
            font-size: 16px;
            pointer-events: none;
            transition: 0.3s;
        }

        input:focus + label,
        input:not(:placeholder-shown) + label {
            top: -8px;
            left: 10px;
            font-size: 12px;
            color: #ff4d4d;
        }

        button {
            padding: 14px;
            background-color: #b30000;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background-color: #800000;
        }

        @media(max-width: 500px) {
            .container {
                padding: 30px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Delete User</h1>

        <div class="readonly-info">ID: <%= user.ID %> (Not Updatable)</div>
        <div class="readonly-info">Username: <%= user.username %></div>

        <p style="color:#b30000; text-align:center; font-weight:bold; margin-bottom:20px;">
            To delete this user, please provide Email and Password
        </p>

        <form action="/user/<%=user.ID%>?_method=DELETE" method="post">
            <div class="form-group">
                <input type="email" id="Email" name="Email" placeholder=" " required>
                <label for="Email">Email</label>
            </div>

            <div class="form-group">
                <input type="password" id="password" name="password" placeholder=" " required>
                <label for="password">Password</label>
            </div>

            <button type="submit">Delete User</button>
        </form>
    </div>
</body>
</html>
